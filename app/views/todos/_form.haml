.form-inline
  = simple_form_for([@list, @todo], url: list_todos_path(@list), method: :post, html: { class: 'form-inline' } ) do |f|
    = f.input :task, autofocus: true, label: false, input_html: { size: 70 }, placeholder: 'New todo'
    = button_tag (f.button :submit), class: 'btn btn-primary', data: { disable_with: "<i class='fa fa-spinner fa-pulse'></i>"} do
      %i.fa.fa-plus
